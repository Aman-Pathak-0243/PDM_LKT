Note 
(a). Prepare a Final checklist md file, for the tasks that i am giving, and when the task is done, update the checks for that.
(b). Task 1 will be done into one claude session, and remaining tasks into another after complition of task 1. So session one will also give a proper starter message prompt to start the next session, before ending.
(c). for markdown files: if any markdown file(s) that i will ask you to make, relevant md file is already there and update that with that details. If not then create the file.

1.  Now quickly analyze the entire codebase, modules in great detail and check that all the logics etc everything is correct or not, like the methodologies how we are checking the health of modules, what panels are used and are those panels relevent or not etc. If there is any logical error inside any module's codebase then fix it, also check how the root cause analysis etc we are doing is insightful or not, if not make it insightful. Also untill i dont have the db access, we will store the data into csv file format so proper folder for that should be there, and an env variable to toggle between csv files method and mysql database method. After doing everything, write "done master".
2. Then prepare a markdown file in which how a operator will operate this system, what things he will have to monitor regularly and where is that thing inside the system so he will easily navigate to that thing to monitor, like SOP of operator. What are his regular and interval tasks etc.
3. one markdown file in which it will be mentioned what resources will be required to host it like mysql db size etc
4. one markdown in which in which all the details a developer should know to develop, maintain, update the code, will be listed, also if he has to use another big database for the system if one database is full, then how he will make the backup of the data and export into another database, write a script also for doing it and document this feature as well wherever relevent.
5. One markdown file in which how this system is built etc will be written, what we are tracking, what value it will add, etc will be writtn.
6. One markdown file in which all the urls of the systems will be listed and where they directs, what are the features etc.
7. one markdown in which the methods of checking the health of the modules will be listed so that anyone can know how our system and its modules are working. like for eg. in shuttle module, which dashboards and panels are we using, what fields of the panels are useful and for what, and how are we using those fields to make the algo of calculating the health status of that module.
8. Then Prepare compile everything and write a Word notebook from all the markdown files that we have with proper references and contants.
